# Concepts

The *guides* section will tell you how to do a thing. This section explains the
machinery underneath, because in this problem space the machinery is the
difficulty.

- **[The OPC model](opc-model.md)** — what a `.pptx` actually is: parts,
  relationships, the three separate identifiers all called "id," and precisely
  why the slide-copy recipes in circulation produce broken decks.

More pages arrive with the modules they document. The nav here is deliberately
short: under `mkdocs build --strict`, a nav entry pointing at a page that does
not exist is a build failure, so this list can only ever describe pages that
are really there.
